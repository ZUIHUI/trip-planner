# 設備 GPS 天氣取得設定指南

## 功能概述

已整合設備 GPS 與天氣系統，用戶可以啟用 GPS 自動獲取當前位置的天氣資訊。

## 使用方式

### 1. 打開設定面板

在行程詳細頁面右上角點擊 **⚙️ 設定按鈕**。

### 2. 啟用 GPS 定位

在設定面板中：
- 找到 **「啟用 GPS 定位」** 選項
- 點擊切換按鈕啟用 GPS
- 瀏覽器會請求位置權限（第一次使用時）

### 3. 允許位置權限

當瀏覽器提示時：
- 點擊 **「允許」** 允許位置存取
- 或點擊 **「阻止」** 拒絕存取

**注意**: 要使用 GPS 功能必須允許位置權限。

### 4. GPS 位置狀態

啟用 GPS 後，右下角會顯示實時狀態：

```
📍 正在定位中...      // GPS 正在獲取位置
✓ 新宿               // GPS 已定位成功
精度: 65m
❌ 定位失敗          // 定位出現問題
```

## 天氣優先級邏輯

啟用 GPS 後，天氣顯示的優先級為：

1. **🔴 選中的行程地點**（最高優先級）
   - 用戶點擊行程卡片時
   
2. **🟠 第一個行程的地點**
   - 當天未選中任何行程時
   
3. **🟡 當前 GPS 位置**（GPS 啟用時）
   - GPS 成功定位後使用
   
4. **🟢 住宿地點**（預設備用）
   - 最終備用位置

## GPS 位置如何影響天氣

### 場景 1: 已選中行程
```
行程選擇: 淺草
結果: 顯示淺草天氣 (即使 GPS 位置在新宿)
原因: 選中的行程優先級最高
```

### 場景 2: 未選中行程，GPS 已定位
```
GPS 定位: 新宿 (精度 65m)
第一個行程: 銀座
結果: 顯示新宿天氣
原因: 無選中行程，使用第一個行程但 GPS 優先級更高
```

**等等，這裡需要澄清**: 實際優先級是:
1. 選中行程 > 2. 第一個行程 > 3. GPS 位置 > 4. 住宿地點

### 場景 3: GPS 禁用
```
GPS 狀態: 已禁用
結果: 使用第一個行程地點或住宿地點
說明: GPS 功能完全關閉
```

## 技術細節

### GPS 工作流程

```
用戶啟用 GPS
    ↓
請求瀏覽器位置權限
    ↓
若允許 → 獲取 GPS 坐標 (lat, lon)
若拒絕 → 顯示錯誤信息
    ↓
反向地理編碼 (坐標 → 地點名稱)
    ↓
將坐標傳遞給天氣 API
    ↓
獲取高精度天氣數據
```

### 位置反向編碼

GPS 坐標會自動轉換為地點名稱：

```javascript
GPS: { lat: 35.6895, lon: 139.7037 }
  ↓
地點: 新宿
  ↓
天氣 API: 使用坐標查詢 (更精準)
```

### 支援的地點自動識別

系統可自動識別的位置：

**台灣**:
- 台北、台中、高雄、新竹、台南

**日本** (東京和近郊):
- 新宿、澀谷、銀座、淺草、秋葉原、原宿等
- 晴空塔、東京迪士尼、富士山等景點
- 羽田機場、成田機場等

## 天氣數據來源

當使用 GPS 定位時，天氣服務會：

1. **直接使用坐標查詢** (最精準)
   ```
   API 參數: lat=35.6895, lon=139.7037
   精度: ±11 公里
   ```

2. **比地點名稱更精準**
   ```
   地點方式: 新宿 (某一點)
   GPS 方式: 精確到當前位置
   ```

3. **支援任何坐標**
   ```
   不限於預設地點清單
   可查詢任意位置的天氣
   ```

## 注意事項

### ⚠️ HTTPS 要求

GPS 功能需要 **HTTPS 連接** (安全連接)：
- ✅ 生產環境: 自動使用 HTTPS
- ✅ 本地開發: localhost 除外
- ❌ HTTP 不支援 GPS API

### ⚠️ 隱私考慮

- 用戶位置只在瀏覽器中計算
- **不會上傳到服務器**
- 只用於本地天氣查詢
- 用戶可隨時禁用位置共享

### ⚠️ 電池消耗

啟用 GPS 定位會：
- 增加設備電池消耗
- 建議在需要時才啟用
- 返回列表頁面不會重置 GPS 設定

### ⚠️ 定位耗時

首次 GPS 定位可能需要：
- 戶外: 2-5 秒
- 室內: 5-15 秒（可能失敗）
- 後續定位: 更快 (使用快取位置)

## 使用案例

### 案例 1: 旅行中實時天氣
```
情況: 用戶在東京旅行，走到新地方
操作: 啟用 GPS
結果: 自動更新當前位置的天氣
```

### 案例 2: 規劃行程時
```
情況: 用戶在家規劃下周的旅行
操作: GPS 禁用（在家時不需要）
結果: 使用行程地點或住宿地點天氣
```

### 案例 3: 多地點行程
```
情況: 當天去多個景點
操作: 啟用 GPS + 點擊行程卡片
結果: 到達景點時自動顯示該景點天氣
```

## 常見問題

### Q: GPS 定位失敗怎麼辦？
**A**: 
1. 檢查位置權限是否已允許
2. 確保有網路連接
3. 嘗試在戶外重新啟用
4. 重新整理頁面重試

### Q: GPS 顯示的位置不對？
**A**:
1. 精度範圍是 ±11 公里，城市內通常 ±100 米
2. 室內定位不精準，建議到戶外
3. 快取位置 1 分鐘，可能顯示舊位置

### Q: 如何關閉位置共享？
**A**:
1. 打開設定 → 禁用 GPS
2. 在瀏覽器設定中撤銷位置權限
3. 返回到 TripListPage 時不會保留 GPS 狀態

### Q: GPS 會消耗多少流量？
**A**: GPS 只在本地工作，不消耗網路流量。但查詢天氣需要網路。

### Q: 可以在後台保持 GPS 定位嗎？
**A**: 頁面在背景時 GPS 會自動暫停。重新進入頁面後恢復定位。

## 調試和日誌

啟用 GPS 時，控制台會顯示詳細日誌：

```
🌍 開始獲取天氣: { dateStr: '12/03', hasGPS: true, coords: {...} }
📍 GPS定位成功: { latitude: 35.6895, longitude: 139.7037, accuracy: 65 }
📍 反向地理編碼結果: { closestLocation: '新宿', distance: 0.45 }
✅ API 數據成功: { avgTemp: 15, weatherCode: 0 }
```

## 技術棧

- **GPS API**: Geolocation API (原生 Web API)
- **位置轉換**: 自建反向地理編碼（距離最近地點）
- **天氣 API**: Open-Meteo (免費、無需 Key)
- **定位精度**: ±11 公里網格

## 文檔參考

- 詳細天氣 API 文檔: `WEATHER_GPS_INTEGRATION.md`
- 天氣修復報告: `WEATHER_FIX_REPORT.md`
- 技術整合總結: `INTEGRATION_SUMMARY.md`
