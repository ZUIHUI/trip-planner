# 行前清單新功能 - 排序與編輯功能

## 概述
為行前清單（Packing List）組件新增了排序和編輯功能，使其功能與購物清單相當。

## 新增功能

### 1. 編輯功能
- **編輯按鈕**：當滑鼠懸停在項目上時，會出現鉛筆圖標的編輯按鈕
- **編輯模式**：點擊編輯按鈕後，項目文字變成可編輯的輸入框
- **保存更改**：
  - 按 `Enter` 鍵保存修改
  - 點擊綠色勾選按鈕保存
  - 提交前進行文字驗證（不允許空白項目）
- **取消編輯**：
  - 按 `Escape` 鍵取消編輯
  - 點擊灰色 X 按鈕取消編輯
- **編輯模式特性**：
  - 編輯時，複選框被禁用
  - 編輯時，拖拽功能被禁用
  - 編輯時，排序手柄（GripVertical）隱藏

### 2. 排序功能（增強）
排序功能已經存在，但進行了以下優化：
- **拖拽排序**：使用拖拽手柄重新排序項目
- **觸摸支援**：支援行動設備的觸摸拖拽
- **視覺反饋**：
  - 拖拽中的項目透明度降低（50% opacity）
  - 懸停時背景色改變
  - 拖拽手柄在懸停時顯示深色

### 3. UI 改進
- **編輯狀態指示**：編輯中的項目有明顯的輸入框邊框
- **按鈕組**：編輯按鈕和刪除按鈕並排顯示
- **色彩編碼**：
  - 編輯保存按鈕：綠色
  - 編輯取消按鈕：灰色
  - 刪除按鈕：紅色
  - 編輯按鈕：藍色

## 技術實現

### 新增 State
```javascript
const [editingId, setEditingId] = useState(null);
const [editingText, setEditingText] = useState('');
```

### 新增事件處理器
- `handleStartEdit(item)` - 開始編輯項目
- `handleSaveEdit()` - 保存編輯內容
- `handleCancelEdit()` - 取消編輯

### 修改的 Icons
在 lucide-react 匯入中新增了：
- `Pencil` - 編輯按鈕
- `X` - 取消按鈕
- `Check` - 保存按鈕

## 使用方式

### 編輯項目
1. 滑鼠懸停在項目上
2. 點擊出現的鉛筆圖標
3. 修改文字內容
4. 按 Enter 或點擊綠色勾選按鈕保存
5. 或按 Escape/點擊 X 按鈕取消

### 排序項目
1. 滑鼠懸停在項目上看到排序手柄
2. 點住排序手柄並拖拽到新位置
3. 放開滑鼠即可完成排序

### 行動設備排序
1. 長按項目上的排序手柄
2. 拖拽到新位置
3. 放開即可完成排序

## 測試清單
- [x] 編輯功能 - 新增 handleStartEdit、handleSaveEdit、handleCancelEdit
- [x] 編輯 UI - 條件式渲染編輯和查看模式
- [x] 排序功能 - 拖拽排序已正常工作
- [x] 按鈕組合 - 編輯、刪除、排序按鈕正確排列
- [x] 代碼編譯 - npm run build 通過

## 兼容性
- 桌面設備：完全支援拖拽和編輯
- 行動設備：支援觸摸拖拽和點擊編輯
- 深色模式：所有功能和顏色都適配深色主題

## 相關文件
- 主要文件：[src/components/PackingListContent.jsx](src/components/PackingListContent.jsx)
- 使用位置：[src/App.jsx](src/App.jsx#L740)
